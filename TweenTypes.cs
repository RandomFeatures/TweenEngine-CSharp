using System;

namespace TweenEngine
{
	public enum TweenTypes
	{
		Start, EndOfDelay, IterationComplete, Complete, Kill, Pool
	}

    public enum PositionType
    {
       NONE = -1,
       X = 1,
       Y = 2,
       XY= 3
         
    }
}

